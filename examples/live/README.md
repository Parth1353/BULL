# Live extraction outputs

This directory is intentionally empty in the submission because live Claude generation requires the reviewer's own API key. Run `pnpm examples:live` after creating `.env.local` to produce LTTS and POCL reports through the full parser, Claude, Zod validation, evidence-reconciliation, and PDF-rendering pipeline.
