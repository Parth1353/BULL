# Live extraction outputs

This directory is intentionally empty in the submission because live Gemini generation requires the reviewer's own API key. Run `pnpm examples:live` after creating `.env.local` to produce LTTS and POCL reports through the full parser, Gemini, Zod validation, evidence-reconciliation, and PDF-rendering pipeline.
