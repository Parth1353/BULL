services:
  - type: web
    name: bull-ai-research
    env: node
    plan: free
    buildCommand: npx -y pnpm@9.15.9 install --frozen-lockfile && npx -y pnpm@9.15.9 build
    startCommand: npx -y pnpm@9.15.9 start
    envVars:
      - key: PORT
        value: 3000
      - key: GEMINI_API_KEY
        sync: false
      - key: DEMO_MODE
        value: "false"
